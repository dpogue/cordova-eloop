Cordova nightly ELOOP bug
=========================

Steps to reproduce
------------------

1. `npm install`
2. `npx cordova@nightly prepare`
3. Look for `ELOOP: too many symbolic links encountered` errors
